apiVersion: v1
kind: Pod
metadata:
  name: simple-pod
  labels:
    app: simple-app
spec:
  containers:
  - name: app
    image: python:3.10-slim
    command: ["python", "-m", "http.server", "8000"]
    ports:
    - containerPort: 8000

